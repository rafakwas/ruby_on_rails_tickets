<h1>Wszystkie wydarzenia</h1>
<ul>
  <% for event in @events %>
		<li><%= link_to event.artist, event %></li>
  <% end %>
</ul>

<td><%= link_to 'Nowe ogłoszenie', new_event_path, class: 'btn btn-success btn-xs' %></td>

