<p id="komunikat"><%= flash[:komunikat] %></p>

<p>
  <strong>Artysta:</strong>
  <%= @event.artist %>
</p>

<p>
  <strong>Min cena:</strong>
  <%= @event.price_low %>
</p>

<p>
  <strong>Max cena:</strong>
  <%= @event.price_high %>
</p>

<p>
  <strong>Data:</strong>
  <%= @event.event_date %>
</p>

<%= render "tickets_list", tickets: @event.tickets %>

<%= render "new_ticket", ticket: Ticket.new(:event_id => @event.id, :user_id=>current_user.id) %>

<td><%= link_to 'Back', events_path, class: 'btn btn-warning btn-xs' %></td>
